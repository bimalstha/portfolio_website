import styled from "styled-components";

const AboutPage = () => {
  return <AboutStyle>AboutPage</AboutStyle>;
};

const AboutStyle = styled.div`
  display: flex;
  height: 100vh;
`;

export default AboutPage;
